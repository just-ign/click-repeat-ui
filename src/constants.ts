// Window Size Constants
export const INITIAL_WINDOW_HEIGHT = 80; // Keep this as the base/recording height
export const MAX_WINDOW_HEIGHT = 500; // Increased Max height for dropdowns
export const INITIAL_WINDOW_WIDTH = 600; // Reduced width
// export const MAX_WINDOW_WIDTH = 600; // Max width before horizontal scroll (Optional)
export const MINIMIZED_DEFAULT_WIDTH = 455;
export const MINIMIZED_WINDOW_HEIGHT = 42;
export const RECORDING_WINDOW_WIDTH = 200; // Further increased width
export const RECORDING_WINDOW_HEIGHT = 50; // Match command bar min-height
export const PLAY_WINDOW_WIDTH = 300;
export const PLAY_WINDOW_HEIGHT = 300;

// Placeholder Data
export const VMS = ["macos-sequoia-vm-1", "ubuntu-docker-large", "windows-gpu-vm"];
export const MODELS = ["claude-3.7-sonnet", "gpt-4o", "gemini-1.5-pro"]; 